# Host Groups

define hostgroup {
        hostgroup_name          ping-servers
        alias                   Pingable Servers
        members                 status
}

define hostgroup {
        hostgroup_name          http-servers
        alias                   HTTP Servers
        members                 community, status, keeper
}

define hostgroup {
        hostgroup_name          https-servers
        alias                   HTTPS Servers
        members                 community, status, github.com, squash, squash-restful-api, squash-bokeh
}
# Stop api.github.com since they get angry about it.

# Squash and CI require logins; some of the others require specific URLs

define hostgroup {
        hostgroup_name          ssh-servers
        alias                   SSH Servers
        members                 community, status
}
