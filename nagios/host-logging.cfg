define host{
        use                     dm-square-server 
        host_name               logging
	alias			logging.lsst.codes
        address                 logging.lsst.codes
        }

define service {
       host_name		logging
       service_description	URL /ping
       check_command		check_https!-u https://logging.lsst.codes/ping
       use			generic-service
       notification_interval	0
}
