define command {
           command_name check_https
           command_line    $USER1$/check_http -I $HOSTADDRESS$ -H $HOSTADDRESS$ -S $ARG1$ -C 60
}

define command {
               command_name check_https_authenticated
               command_line    $USER1$/check_http -I $HOSTADDRESS$ -H $HOSTADDRESS$ -a $USER3$:$USER4$ -S $ARG1$ -C 60
}

define command{
        command_name    check_bitly_proxy_https
        command_line    /usr/local/bin/check_bitly_proxy_https -n sqrbot -p $USER5$ -a $ARG1$ -u $ARG2$ -C 60
}
